[package]
name = "watchpost-shell"
version = "0.1.0"
edition = "2021"
description = "Watchpost desktop shell"

[build-dependencies]
tauri-build = { version = "2.6", features = [] }

[dependencies]
tauri = { version = "2.11", features = [] }
serde_json = "1"

[profile.release]
opt-level = "s"
lto = true
strip = true
